name: IntegrationTest

on:
  workflow_call:
    inputs:
      portalName:
        required: true
        description: suite to run
        type: string
  workflow_dispatch:
    inputs:
      portalName:
        description: suite to run
        required: true
        type: string
      suitesToRun:
        description: "The suites which needs to be run i.e., for single suite run format: ['suiteName'], multiple suite run format: ['suiteName1', 'suiteName2']"
        required: false
        type: string

jobs:
  metadata:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.generate-matrix.outputs.matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: generate matrix
        id: generate-matrix
        run: |
          file_name="list.yml"
          echo "matrix=$(yq e ".suites" "$file_name" | sed 's/^- //; s/^/'\''/; s/$/'\''/' | paste -sd, - | sed 's/^/[ /; s/$/ ]/')" >> $GITHUB_OUTPUT
          

  test:
    needs: metadata
    runs-on: ubuntu-latest
    strategy:
      matrix:
        tests: ${{ fromJson(needs.metadata.outputs.matrix) }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Run tests
        run: |
          echo "Running tests for ${{ matrix.tests }} in portal ${{ github.event.inputs.portalName }}"
