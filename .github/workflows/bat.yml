name: Workflow Test

on:
  workflow_dispatch:

jobs:
  trigger:
    strategy:
      matrix:
        Mapping:
          - portalName: CM7
            suitesToRun: "['1', '2', '3', '4']"

          - portalName: config7
            suitesToRun: "['5', '6', '7', '8']"
#    uses: Dhivaagar/DemoProject/.github/workflows/integration-Bat.yml@main
    uses: ./.github/workflows/integration-Bat.yml
    with:
      portalName: ${{ matrix.Mapping.portalName }}
      suitesToRun: ${{ matrix.Mapping.suitesToRun }}